[package]
name = "leyline"
version = "1.3.3"
description = "Leyline desktop AI gateway"
authors = ["The AI Inc"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
